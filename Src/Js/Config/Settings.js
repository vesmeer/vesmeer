define(

    function() {

        var Settings = {

            /* If true, sounds will be on. */
            SOUNDS: true,

            /* If true, shadows will be rendered. */
            SHADOWS: false,

        }

        return (Settings);
    }
);
