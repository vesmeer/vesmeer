To run the test, use Google Chrome and open it with the --allow-file-access-from-files
option:

    google-chrome --allow-file-access-from-files

Then open the URL: 

    http://vesmeer/Src/Test/index.html

to run the tests and see the output report. Don't open the index.html file directly
by double-clicking on it.

